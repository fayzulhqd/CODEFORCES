#include <bits/stdc++.h>
using namespace std;

int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);

  int t;
   if (!(cin >> t)) return 0;

  while(t--){
    long long n;
    cin >> n;   

    long long res=2*n;

    cout<<res<<endl;
  }


}